fastapi==0.110.0          # Hala bunu tutmanızı öneririm, performansı eşsizdir.
uvicorn==0.27.1           # FastAPI'yi çalıştıran motor
sqlalchemy==2.0.27        # ORM yapımız
# psycopg2-binary==2.9.9    # PostgreSQL veritabanı adaptörü
requests==2.31.0          # LLM'e senkron istek atmak için
httpx==0.27.0             # Telegram'a asenkron istek atmak için
python-dotenv==1.0.1      # .env dosyasını okumak için